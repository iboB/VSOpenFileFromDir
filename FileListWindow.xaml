<Window x:Class="OpenFileFromDir.FileListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vs_shell="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.11.0"
        Title="Open File from Directory" WindowStartupLocation="CenterOwner"
        MinWidth="200" MinHeight="100" KeyDown="Window_KeyDown"
        DataContext="{Binding RelativeSource={RelativeSource Self}}" ShowInTaskbar="False">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<TextBox Name="filterTextBox" TextChanged="filterTextChanged"/>
		<Grid Grid.Row="1">
			<ListBox Name="listBox"/>
		</Grid>
	</Grid>
</Window>
